# Progress

## Current state
**Decision 2026-09-16 — stay local.** Marc asked about a web login + phone app, heard the plan (Mac app becomes the collector that syncs to a hosted SvelteKit/Neon/Vercel service with magic-link accounts, server-side connectors, PWA + push, share links for bookkeeper/insurer, Capacitor later — roughly doubles the codebase and makes him custodian of other people's credentials), and decided: **keep it local-only, distributed from GitHub, for now.** Do not build sync/hosting unless he raises it again. The "local only; nothing leaves the machine" promise stays literally true. That plan is kept here only as the reference for when/if it comes back.

Ramp fully proven live, 2026-09-15 evening: Marc simulated purchases on the agent's card via the sandbox demo-tools form (⌘J → add transactions; Card uuid = the fund's card; the form's "Randomize card uuid" toggle must be off; decision mode `stored_controls` checks against the fund's real limits). Two rows (Postmates 1.89, Bala 4.62) arrived attributed to `claude-code (claude-desktop) / general-purpose: Write-one-file test sub-agent` by credential, reversible 0, and tripped `unattended_irreversible` — correct. Everything in docs/RAMP.md is now live-verified: token, users, funds, issue fund+card, suspend, unsuspend, transaction sync incl. agent-fund attribution. Not yet seen live: a DECLINED row (the form asked for 4, Ramp produced 2 — possibly the declines were not emitted) and terminate (deliberately not pressed; the fund is kept for further tests).

Adjustable alert rules, 2026-09-15 (Marc, after 57 demo-employee purchases tripped "payment over 100": "can we make it so they can set in the App when it fires?"). `alerts.settings(conn)` / `save_settings(conn, form, user)` store `alert.money_threshold`, `alert.money_scope` (agent = not a person's own purchase, i.e. agent or unconfirmed — the default; all = anyone), `alert.burst_threshold`, `alert.on.<rule>` in `meta`; a save that changes anything is a receipt row (`agent-receipt:rules`, human) "Changed alert rules: payment over 250.00 (anyone); 3+ irreversible actions in an hour; off: …; 4 of 5 rules on". `evaluate` reads the settings each run and skips rules that are off; changes apply to actions evaluated from then on (old alerts stay; a saved banner says so). Form on the Needs review page (`POST /alerts/rules`; hidden-0 + checkbox-1 pairs, last value wins). The 57 stale money alerts on Ramp person rows were deleted (alerts are flags, not the record). 209 tests.

Ramp LIVE in the sandbox, 2026-09-15: Marc got sandbox access (demo business "Agent Receipt", owner David Wallace, 43 demo users with marc+…@jobhatch.io addresses, 106 demo funds), created the Developer app (Company → Developer → Apps) with client-credentials + scopes; credentials in .env; cardholder = demo user Pam Beesly (`RECEIPT_RAMP_USER_ID`). Two live corrections: (1) the token endpoint rejects JSON — body must be form-encoded; (2) the sandbox has future-dated transactions, so `ramp_since` is now the last *sync time* (query from since−3 days) rather than the newest transaction time. Proven live: issue fund + card (•••• 5039, $25/day for the test sub-agent), suspend, unsuspend, transaction sync (91 rows). New rule: Ramp purchases NOT on an agent fund are attributed to the named card holder by credential (`human`, note starts "by credential:"); the correlator now leaves any non-log row whose note starts with "by credential:" alone. The 91 rows from the first sync were deleted and re-extracted under the new rule (same idea as a RULES_VERSION reset). Still to see live: a purchase on the agent's own fund (Marc: ⌘J → add transactions on card 5039). 207 tests.

Competitor search, 2026-09-15 (Marc: "are we on the way to build something that's not made yet?"). Result in docs/LANDSCAPE.md: nearest is PortEden (cloud data-firewall proxy, cross-provider timeline of connected-app records, free/$8/$49 — different architecture, blind to local agent activity); Nylas CLI audit (local, Nylas-only); claudit-sec (config inventory, not activity); Pipelock/PipeLab signed action receipts and the "Notarized Agents" paper (protocol-level, developer-facing); IETF draft-sharif-agent-audit-trail (record format with prev_hash chain + human_override — cheap to align our export to); enterprise NHI/kill-switch vendors; payment rails. Nobody found combines local cross-vendor side-effect receipts + human-vs-agent evidence + normie statement + identity lifecycle (name/retire/stop/issue). Demand signal: insurers ending "silent AI" cover at 2026 renewals (ISO CG 40 47/48, shadow-AI exclusions, "what evidence can you produce"). Cowork is excluded from Anthropic's own audit logs (MintMCP) — our coverage there is a real gap-filler. Follow-ups noted, not built: AAT-shaped export; accept Pipelock evidence files via the inbox.

Ramp plumbing (step 4 via a card program the customer already has, Marc's call after the "can Stripe handle enterprise agent cards?" discussion), 2026-09-15. **Verified against Ramp's OpenAPI spec** (downloaded `https://docs.ramp.com/openapi/developer-api.json`, 176 paths; the docs site is JS-rendered so WebFetch sees only a shell — use the JSON/`llms-*.txt` files): the old `cards/deferred/virtual` endpoint no longer exists; the primitive is the **fund** (`POST /developer/v1/funds` with `user_id`, `display_name`, `spending_restrictions{limit{amount cents, currency_code}, interval DAILY|WEEKLY|MONTHLY|QUARTERLY|YEARLY|TOTAL, transaction_amount_limit, allowed/blocked category codes}`, `permitted_spend_types{virtual_card, physical_card, reimbursements}`; a fund issues a virtual card automatically; `POST/DELETE /funds/{id}/suspension`, `DELETE /funds/{id}`; transactions carry `fund_id`/`card_id`, `state` incl. DECLINED + `decline_details.reason`, `user_transaction_time`; token = `POST /developer/v1/token` HTTP Basic + JSON `grant_type=client_credentials`; sandbox `https://demo-api.ramp.com` / `https://demo.ramp.com`, ⌘J demo-actions panel to add transactions; Ramp also has an early-access "agent cards" product and an MCP server, not used). Built: `config.ramp_settings` (sandbox default, `RECEIPT_RAMP_USER_ID` = the Ramp user who holds agent cards), `store.ramp_funds` table, `src/ramp_connector.py` (`token` cached, `api`, `list_all` via `page.next`, `issue_fund` → fund named `Agent · <name>` virtual-card-only + receipt row, `set_fund_state` suspend/unsuspend/terminate, `refresh_funds`, `sync` → purchase rows: on an issued fund → source `log`/attribution `agent` "by credential", else source `card` under `ramp card (sandbox)`; declines recorded as `DECLINED: <merchant>` reversible 1; meta `ramp_since`; coverage row; `list_users`), Stop controls per fund (suspend/unsuspend/terminate; Freeze all suspends only), Agents page "Give a card" form (`POST /agents/card`, disabled until `RECEIPT_RAMP_USER_ID` is set) + fund/state line per agent, run.py sync, coverage line, doctor/front page, `.env.example`, `examples/ramp_users.py`, docs/RAMP.md with the setup steps only Marc can do (request sandbox access, create the app with `funds:read funds:write transactions:read users:read cards:read`, pick the cardholder user). **Not live-proven**: no Ramp credentials yet; first real call may show the token endpoint wants form-encoding (one line) — see docs/RAMP.md "Honest limits". 207 tests.

Stop panel (step 3, the kill switch), 2026-09-15. `src/stop.py`: `controls(env)` builds the list from the configuration (no secrets in output) — link-type: Gmail sign-out-everywhere + app-password revoke, Workspace suspend, roll Stripe key, Cowork schedule steps, macOS Automation; api-type: `stripe_freeze` / `stripe_cancel` (GET /v1/issuing/cards?status=active then POST status=inactive|canceled; cancel is `danger`, reversible 0), `google_token` (POST oauth2 revoke with the refresh token, delete `~/.agent-receipt/google_token.json` — **blinds the receipt, does not stop the agent**, and the page says so with an effect pill); process-type: `codex_processes` / `claude_processes` (`pgrep -x` + `pkill -x`; the claude one warns it ends the very session you may be talking to). `run(conn, id|"all", user, …)` executes with injectable `stripe_api`, `post`, `token_path`, `end_processes`; every press — including failures and link "Mark as done" — is recorded as an `agent receipt` / source `input` / human row with `agent-receipt:stop:<id>` (source_ref carries a uuid suffix — same-millisecond presses collided in tests). `presses(conn)` lists them. Page `/stop` (nav tab **Stop**): Freeze all (skips permanent), one section per agent (plain names), effect pills (stops the agent / blinds the receipt / stops it for now), Open ↗ + Mark as done for links, confirm() on the rest, Presses history; `POST /stop/run` → redirect with msg/ok. docs/STOP.md, README. Not live-proven: no Issuing cards on Marc's sandbox and the mailbox is consumer Gmail; unit-tested with fakes. Step 4 (provisioning credentials from the app) remains future. 201 tests.

Plain agent names + nicknames, 2026-09-15 (Marc: "are all agents easily identifiable? I can't see where it says it"). `agents.display(name, nicks, known)` → {name, sub, kind, raw, nickname}: Claude Code / in the Claude app (or in the Terminal); Sub-agent of Claude Code / <task> · where; Cowork · scheduled task; Codex (OpenAI); Google account / <email>; connector labels as set; inbox names as declared; `agent receipt` → "Agent Receipt / a button you pressed". Nicknames: new `agent_names` table, `set_nickname()` (recorded as an `agent-receipt:name` row), `POST /agents/name`, name box per row on the Agents page. Applied everywhere via a Jinja global `agent_display` (context processor, one nickname read per request): statement rows (the "Who did it" column is now ALWAYS shown, with the kind pill and the raw label as a tooltip), Agent chips, the Filtered line, Needs review, Money, the daily summary's "Agents:" line (`summary.agent_names(conn)` → `summary_text(..., names)`), and the CSV (new `agent_label` column right after `agent`; raw kept). docs/AGENTS.md has the mapping table. 196 tests.

Sub-agent visibility follow-up, 2026-09-15 (Marc: "how do I determine it's a subagent?"). A second test worker (general-purpose, wrote one scratch file in the session scratchpad) now appears as its own identity `claude-code (claude-desktop) / general-purpose: Write-one-file test sub-agent` on the Agents page and as a chip (prefixed ↳) on the day statement; worker rows carry a "sub-agent" pill next to the agent name. Also fixed describe: a leading `cd "<project>" &&` is dropped from the plain-English line even when shell parsing fails, and a heredoc (`python - <<'EOF' …`) is described by the command in front of it plus "(with an inline script)" instead of narrating the script body. 194 tests.

Sub-agent identity (step 2), 2026-09-15. Ground truth obtained by spawning one read-only test sub-agent with Marc's permission (before that, all 10,999 Claude Code lines on this Mac had `isSidechain:false` — no sub-agent had ever run here; the 12 "Agent" hits were tool definitions in prompt attachments). Layout: `<project>/<session>/subagents/agent-<agentId>.jsonl` + `agent-<agentId>.meta.json` (agentType, description, toolUseId, spawnDepth); lines carry `isSidechain:true`, `agentId`, `sessionId`=parent, `cwd`, `attributionAgent`; parent has the `Agent` tool_use (description/prompt/subagent_type) and a result with agentId. Implemented: `_claude_code_meta(path)` reads the sidecar; `_claude_code_agent` labels workers `claude-code (<entrypoint>) / <agentType>: <description>` (fallback `/ <role> <id8>`); `classify` now records the parent's `Agent` call as `other` "Explore: <description>" → describe "Started a sub-agent: …"; raw_json gains `agent_id`, `parent_session`, `spawned_by` (build_action takes `meta`); `agents.kind_of` → kind "sub-agent"; RULES_VERSION 9 (re-extract done, spawn row present on today's statement; the test worker only Read so it has no rows of its own — that's correct). docs/SOURCES.md and docs/AGENTS.md updated. 192 tests. Next: step 3, the kill-switch panel.

Agents page + Retire, 2026-09-15 (step 1 of Marc's "identity / remove an agent / revoke access" request; his rule: removing an agent must never remove the record). `src/agents.py`: `known_identities()` (labels from configured connectors, no secrets), `kind_of()` (local agent / mailbox / card / calendar / google account / declared), `list_agents()` (per-identity stats incl. configured-but-quiet ones; retired sort last), `retire()`/`restore()` (new `agent_status` table; each press is written to `actions` as agent `agent receipt`, **source `input`**, attribution human — correlator now treats source `input` as "a button in Agent Receipt was pressed" and reconcile ignores it because it is not a transcript source), `history()`. New alert rule `retired_agent_acted` (action timestamp after retired_at). `/agents` page + `POST /agents/status`; Agents nav tab; statement Agent chips read "name · retired"; `describe` handles `agent-receipt:*` tools. docs/AGENTS.md states plainly that retiring does not stop the agent. **Plan agreed with Marc:** step 2 = check whether Claude Code transcripts mark subagent work (sidechain) so workers can be listed as `claude-code / worker-N` (kind_of already splits on " / "); step 3 = kill-switch panel — real API calls where the issuer has one (Stripe Issuing card freeze/cancel, Google Workspace user suspend, revoking the receipt's own tokens), deep links + steps elsewhere (consumer Gmail, Stripe keys), confirm step, every press a receipt row, "Freeze all"; step 4 (later) = provisioning agent credentials from the app so every agent is stoppable by construction (needs Issuing activation + a Workspace domain — Marc-only setup). Neither Issuing nor Workspace exists in Marc's test setup, so step 3's instant paths can be unit-tested but not live-tested yet. 191 tests.

Money page + bookkeeping export, 2026-09-15 (Marc asked whether the financial rows could feed TurboTax etc.; agreed answer: no direct tax-software import — the amounts are already on the bank/card/Stripe statement and would double-count; the receipt's value is *who/why*, which goes to the bookkeeper). Built: `/money` (and `/money/<start>/<end>`) — rows with an amount only, grouped by month newest-first, spent/received/net tiles per currency, each row linked to its day, print-ready; new **Money** nav tab. `queries.money_summary(stats)` (direction: purchase = out, anything else with an amount = in; signed amounts; per-month and overall per-currency totals). `/export-money.csv?start&end&currency=` → `export.money_csv`: the three-column QuickBooks/Xero bank-import layout (Date `MM/DD/YYYY`, Description, Amount; out negative; chronological; one currency per file; deliberately **no footer** because the importers reject extra rows — the checksummed copy is the full export.csv). Description = what · agent · project (omitted when unknown) · attribution · ref; a leading "Moved money:" becomes "Paid:"/"Received:". docs/BOOKKEEPING.md explains the match-don't-add workflow; README updated. Not built on purpose: QuickBooks Online API memo-writing (needs per-user OAuth; wait for someone to ask). Marc's stance discussion (same day): usefulness hinges on people who must show a third party — insurer, client, auditor — what the AI did; validation = show the Needs-review page to ~5 people who let agents touch money/customers and see if anyone asks "can I send this to my insurer/client". 188 tests.

Two bugs from Marc's "calendar (1) but I can't see it" report, 2026-09-15: (1) the Mac calendar script returned times relative to "now" measured in two different clocks → ~15 s drift → every event looked modified on the second poll → six bogus "Changed:" rows (deleted, Mac baseline reset); script now emits absolute Unix seconds (`date +%s` + Calendar's `current date` read at the same instant), change tolerance 2 s. (2) `reconcile()` deleted the credential-attributed Google row because it has source='log' but no tool-call to classify — reconcile now skips rows whose raw source isn't a transcript source. Plus UX: when Show chips hide rows, the Actions/Unexplained sections say "N rows hidden by your Show settings — show all categories".

Google Calendar LIVE, 2026-09-15: Marc created a Google Cloud project "Agent Receipt", enabled the Calendar API, configured the (new-style) consent screen with `marcsagenttest@gmail.com` as a test user, created a Desktop OAuth client; client id/secret in `.env`; one-time sign-in done as the agent account; token at `~/.agent-receipt/google_token.json`. First sync hit Google's 410 "minimum modification time too far in the past" on a fresh calendar → fallback to an unbounded listing (fixed + tested). Live test: an event Marc added while signed in as the agent account appeared as `create_event` at Google's real creation time, attributed `agent` = `google calendar (marcsagenttest@gmail.com)` by credential, with a link. Mac Calendar connector also on; its Automation prompt for Agent Receipt still pending Marc's click.

Google Calendar plumbing, 2026-09-15 (built at Marc's request). `src/google_calendar.py`: stdlib-only OAuth (auth URL, code exchange, refresh; token at `~/.agent-receipt/google_token.json`, mode 600) + Calendar API v3 incremental sync (`syncToken` per calendar in meta, `showDeleted`, `singleEvents`, paging, 410 → silent re-baseline). Rows: create_event at Google's real creation time with a link to the event; changed/deleted like the Mac connector. **Attribution by credential:** events whose `creator.email` is in `RECEIPT_GOOGLE_AGENT_EMAILS` are written as source `log`, attribution `agent`, label `google calendar (<email>)` — the agent's own Google account, same principle as its mailbox/card. `examples/google_calendar_auth.py` runs the one-time consent on 127.0.0.1:8766. Setup steps (Cloud project, enable Calendar API, consent screen + test user, Desktop OAuth client) in docs/GOOGLE_CALENDAR.md — only Marc can do those. Shares `calendar_seen` with the Mac connector under a `gcal:` prefix (the Mac connector's deletion scan skips those). 183 tests.

Calendar connector, 2026-09-15 (Marc chose calendar as the next integration). Reads the macOS Calendar app via **AppleScript/osascript** (`src/calendar_connector.py`): new/changed/deleted events → rows with source `calendar`; first run baselines; `calendar_seen` table + `meta.calendar_baselined`; opt-in `RECEIPT_CALENDAR=on` (set in Marc's .env); default skips holiday/birthday/Siri calendars; each poll ~15 s. **EventKit dead end, fully explored — don't retry:** on this macOS (Darwin 25) `requestFullAccessToEvents` returns granted=False with no error and no prompt for a Python process; tried injecting `NSCalendars*UsageDescription` into the in-memory bundle info, pumping the run loop, a helper .app bundle with the keys in its Info.plist using a symlinked interpreter, an ad-hoc re-signed copied interpreter (a plain copy is SIGKILLed by code signing), and launching that helper via `open` — all refused. Only a compiled, signed executable would work; not worth it. AppleScript prompts normally ("Agent Receipt wants access to control Calendar"). Migration: `actions.source` CHECK now includes 'calendar'; store._migrate rebuilds the table with an explicit shared-column list. pyobjc-framework-EventKit removed from requirements. Limits documented in docs/CALENDAR.md (no creation time → detection-time stamps).

Follow-ups 2026-09-15 from Marc's screenshots: By-day is a grid of day tiles; section order Summary → Totals → Actions → By day → Keyboard → Unexplained; dark mode is a real palette (Marc browses in dark mode) — the `@media (prefers-color-scheme: dark)` block must stay at the END of base.html's stylesheet or the light rules for the header, flagged rows and hover win and text becomes unreadable (that bug shipped once); muted/faint greys raised to ≥4.5:1 contrast in both themes.

App-style redesign, 2026-09-15 (Marc: "make it look nicer as if it were an app for a normie… nice font and coloring… hide categories"). `base.html` now has a sticky top bar (wordmark, nav pills Today/7 days/30 days/All time/All days, a "N need review" badge), cards on a warm off-white ground, the system font, one indigo accent, stat tiles, calmer tables; print styles kept. Category labels for people (`TYPE_LABELS`/`type_words` in queries.py: "File edits", "Commands run", "Emails sent", "Purchases", "Browser & other"); attribution pills read agent / person / unexplained. A **View** row of check-chips shows/hides each section and a **Show** row hides categories of rows — client-side, no reload, remembered in localStorage (`ar.hiddenSections`, `ar.hiddenTypes`); groups with no visible rows hide themselves. Server-side `?type=`/`?agent=`/`?user=` filters still work. Summary "By type" uses words. reconcile() now fills empty-string users too. 175 tests.

Plain-English lines, 2026-09-14 (Marc: "the What section is still way too technical"). `src/describe.py` turns every row into one sentence — "Edited src/alerts.py", "Committed changes to git", "Deleted tmp", "Sent an email to …", "Paid 150.00 USD at ACME", "Used the browser: opened a page, clicked, typed" — by tokenizing shell commands segment by segment with a verb table for git/pip/npm/python/curl/rm/mkdir/etc. Shown as the main line on statements, Needs review, notifications, and a `what` CSV column; the raw command/path stays underneath in small grey mono (collapsed when long). 174 tests.

Menu-bar app, 2026-09-14: `Agent Receipt.app` is a thin bundle (Info.plist with LSUIElement + a zsh launcher in Contents/MacOS) that lives inside the project folder, creates `.venv` on first run if needed, and runs `src/menubar.py` — a `rumps` app (🧾 icon with the Needs-review count; menu: Open statement, Needs review, Status…, Quit) that owns the input-monitor and page child processes and refreshes/verifies in a background thread (`Runner`). `make_app.command` rebuilds the bundle; the bundle is committed so ZIP downloads include it. rumps==0.4.0 added to requirements. First launch from Finder triggers macOS "python3.14 wants to access files on your Desktop" (the project lives on the Desktop) — allow; and Accessibility must be granted to "Agent Receipt" (or the Python it runs) instead of Terminal. Launcher output when run by hand is clean apart from Flask's dev-server banner. Gmail app password for the test mailbox was invalidated by Google the same evening (`Invalid credentials`) — Marc to generate a new one. 158 tests.

Installable by a second person, 2026-09-14: `Setup.command` (checks Python ≥ 3.11, creates `.venv`, installs pynput + flask, copies `.env.example` → `.env`, prints the doctor report and next steps; safe to re-run) + `src/doctor.py` (agents found with transcript counts/MB, connectors configured, Accessibility permission for the launching app via `AXIsProcessTrusted`, DB/actions/needs-review, whether the monitor is running from the coverage heartbeat). The same status renders as a "This machine" table at the bottom of the front page. `Agent Receipt.command` now points to Setup.command when `.venv` is missing. README rewritten as a five-minute "Try it" for a newcomer (ZIP download, right-click → Open for unsigned .command files, the one permission). Verified by cloning into a scratch folder and running Setup.command non-interactively. 156 tests.

Issuing own-card test: CLOSED as blocked by Stripe, 2026-09-14. The sandbox financial account stays `pending` until the account completes live onboarding ("Create your Stripe profile" → "To continue, switch to live mode. This feature can't be set up in your sandbox account."). Marc stopped there, correctly — not doing live business verification on the Jobhatch account for a demo. Code verified up to that gate. Don't retry without an Issuing-activated account.

Earlier detail: Marc enabled Issuing via "Explore in sandbox" (sandbox "Coral Slide", `acct_1UFgjNIwSpLuvNxC`, its own key now in `.env`). Learned the hard way (all in docs/STRIPE.md): sandbox keys differ from classic test keys; v2 API needs a `.preview` Stripe-Version (`request_v2` added, default `2026-08-26.preview`); cards need `financial_account_v2=`; cardholders need first/last name, dob, terms acceptance. Remaining blocker: the sandbox's v2 financial account is `status: pending` → "cannot create a new card … Please try again with an open FinancialAccount". Asked Marc to complete the sandbox **Setup guide**; then rerun `examples/simulate_card_purchase.py` (fund the account first — funding route for v2 accounts still to be found; `/v1/topups destination_balance=issuing` is rejected). Three duplicate incomplete test cardholders exist in the sandbox (harmless). Charges path already proven.

Stripe connector LIVE in test mode on the BigSEOBoost account (`acct_1TJuLmRAiVWZq5e0`, key in `.env`). Issuing is NOT enabled on that account ("not set up to use Issuing"), so the own-card simulation can't run yet — Marc can enable it at dashboard.stripe.com/issuing/overview if he wants that path; charges work. First sync imported 12 existing test-mode charges from May–July (Jobhatch pay-link tests incl. two $49 test subscriptions still billing on the 18th), all `unknown`. Live run 2026-09-14: `examples/simulate_charge.py` $150 → `human` (Marc was typing) + money alert + notification; `--declare` $42.10 → merged, `agent`. Money-alert wording now says "paid" for purchases and "moved" for collected charges. Both connectors (mailbox + Stripe) are proven end to end; the v2 design's "agent has its own mailbox and card" is real on this machine.

Stripe connector built 2026-09-14. `src/stripe_connector.py` (urllib, no SDK) polls `/v1/issuing/authorizations` (agent's own card → `purchase`, source `card`, Stripe event time, dashboard link, last4 only) and `/v1/charges` (money the agent collected → `other` with amount, reversible=refundable). Position = newest `created` per mode in `meta`; Stripe ids are source_refs so receipt-line declarations merge. Issuing-not-enabled is handled (charges still recorded). `examples/simulate_card_purchase.py [amount] [merchant] [--declare]` creates a test cardholder + virtual card once and authorizes via Stripe's test helper; refuses live keys. Coverage row + docs/STRIPE.md. Wired into `run.refresh`. 153 tests.

Email + card connector LIVE against the test mailbox `marcsagenttest@gmail.com` (Marc created it 2026-09-14; app password in the git-ignored `.env`; IMAP is always-on for new Gmail accounts, no toggle). End-to-end results: Marc's hand-typed email → `human` (93 input events in prior 30 s); `send_test_email.py --declare` → `agent` (receipt line merged with the mailbox observation); `send_fake_card_alert.py` → `purchase 150.00 USD`, money_over_threshold alert + macOS notification. Two fixes from the live run: (1) `_merge_declaration` in log_parser — a receipt line whose id matches an action already observed via email/card flips that row to source `log`, attribution `agent`, note "…also observed via email" (declaration wins, observation kept); (2) card rows use `card:<message-id>` as source_ref so a charge never collides with the email announcing it (bit us because the fake alert was self-sent and also sat in Sent). Known limitation: a card row's timestamp is the alert email's Date, not the charge time, so the 30 s attribution window measures against when the bank mailed, not when the card was used. Runner restarts are needed after code changes; the sqlite3 CLI needs `.timeout` while the runner is refreshing.

v2 email + card connector built 2026-09-14. `src/config.py` reads a git-ignored `.env` (see `.env.example`); `src/email_connector.py` polls the agent mailbox read-only over IMAP (stdlib): Sent folder → `send_email` rows (source `email`), alerts folder → `purchase` rows (source `card`) via `src/card_alerts.py` (Chase/Amex/Capital One/generic phrasings; last four digits only, never card numbers). Position tracked per folder by IMAP UID in `meta`; message-id dedups. Attribution left to the correlator (first rows that can come out human/unknown). Wired into `run.refresh` (never raises), coverage table rows switch to "covered" when configured. `examples/send_test_email.py [--declare]` and `examples/send_fake_card_alert.py` exercise the pipeline end to end; setup in docs/EMAIL.md. Marc's decision: test against a real throwaway mailbox and Stripe test mode rather than fakes. Next: Marc creates the mailbox + app password, fills `.env`, we run the three test scripts; then Stripe test mode (Issuing if enabled). 147 tests.

Multi-harness shipped 2026-09-14. Marc's framing: the model provider (OpenAI/Anthropic/Google/OpenRouter) is irrelevant; what matters is the *harness* that acts and whether it leaves a record. log_parser now has a `Call` abstraction (tool name + input + optional overrides) and each `Source` declares an `extract(entry, meta)` plus a `line_hint` prefilter. Sources: claude-code, cowork, **codex** (`~/.codex/sessions/**/*.jsonl`, `event_msg`/`item_completed` items: CommandExecution, FileChange add/update/delete, McpToolCall, Extension savedPath; label `codex (<originator>)`), and **inbox** (`~/.agent-receipt/inbox/*.jsonl` — the open "receipt line" format in docs/RECEIPT_LINE.md, helpers in examples/receipt_line.py and .js; note reads "declared by the agent itself"). Google Antigravity found (`~/.gemini/antigravity`, protobuf conversations) — not readable, listed as not covered. Codex quirks learned from real data: commands are argv lists wrapped as `/bin/zsh -lc '<cmd>'` (unwrapped before classifying/showing), and `FileChange.changes` entries are sometimes bare path strings. A parse error in one transcript now logs to stderr and skips that file (retried next refresh) instead of killing the runner — the first Codex attempt crashed the whole loop, which is how that gap was found. RULES_VERSION 8. 139 tests.

Local alerts shipped 2026-09-14. `src/alerts.py`: four rules evaluated on every refresh over not-yet-seen actions (`meta.alerts_evaluated_to`), stored in `alerts` (one per action+rule, `seen_at` for review): unattended_irreversible (reversible=0 by a scheduled task, or with no physical input in the prior 30 s), irreversible_burst (5+ in an hour by one agent), money_over_threshold (>100; needs v2), unknown_attribution (needs v2). Fresh matches (action < 24 h old) raise a macOS notification via osascript (≤3 individually, else one batched); older ones just wait. `/alerts` page ("Needs review") with mark-seen; a "Needs review: N" badge on every page; flagged rows highlighted on statements; "Needs review:" line in summaries. Rules version bump also clears alerts so they're re-evaluated.

MCP classification: no email/calendar/purchase tool has ever been called on this machine (full tool census run 2026-09-14). Added `MCP_TYPE_PATTERNS` (suffix regexes → send_email / create_event / purchase / post) for when such connectors appear, and fixed real misclassifications: computer-use wait/zoom/scroll and Cowork present_files/request_access/request_cowork_directory are no longer logged. RULES_VERSION 6. 136 tests.

Log integrity shipped 2026-09-14. Cowork's own `_audit_hmac` could not be reproduced (details and everything tried in docs/SOURCES.md — don't retry without new information). Instead the receipt keeps its own record: `_NewLines` hashes every byte it streams and `ingest` stores one row per consumed range in `transcript_chunks`; `src/verify.py` re-reads those ranges and compares, storing the result in `meta.integrity`. The runner verifies at start and every 6 h; every page and the summary carry the "Log integrity: … unchanged since the receipt first read them" line, with the explicit caveat that it cannot prove the log was honest at first read. RULES_VERSION 5 (chunks needed a re-extract). 130 tests.

Range statements, CSV export, and print shipped 2026-09-14. `queries.statement(start, end, filters)` is the one query; the day page is the single-day case. Routes: `/day/<d>`, `/range/<start>/<end>`, `/week` (last 7), `/month` (last 30), `/all` (from earliest action); all take `?type=&agent=&user=`. Range pages add a by-day table and use date+time stamps. `/export.csv?start=&end=&type=&agent=&user=` (`src/export.py`) writes the full target (not the clipped one), then a footer with range, filters, row count, the coverage table, monitor uptime, and a SHA-256 of the data section plus the exact `head -n N | shasum -a 256` command to verify it. `base.html` has an `@media print` stylesheet: chrome hidden, long targets printed in full, page-break rules — File → Print / Save as PDF is the paper statement. Template renamed day.html → statement.html. 126 tests.

Marc's product decisions (2026-09-14): CSV yes (Excel/Sheets open it); native Excel/Google Sheets no (Sheets means OAuth + data leaving the machine — v3); QuickBooks push no — the receipt has no money rows and its value on transactions is the agent/human tag, which is a reconciliation export, not an integration.

Second agent source shipped 2026-09-14: **Cowork** (Claude Desktop's local agent mode, incl. scheduled tasks) — `~/Library/Application Support/Claude/local-agent-mode-sessions/**/audit.jsonl`, 128 sessions / 5.9 GB, details in docs/SOURCES.md. log_parser now has a `Source` abstraction (`SOURCES = (CLAUDE_CODE, COWORK)`): per-source root/glob, agent label, timestamp field (`_audit_timestamp` for Cowork), and session metadata (sidecar `local_<id>.json` supplies the title used as the project label, since Cowork's cwd is a VM path). `mcp__workspace__bash` (Cowork's sandbox shell) is treated like Bash. Transcripts are streamed and only lines containing `"tool_use"` are JSON-parsed: the first full pass over 5.9 GB took 9.3 s. Database: 1,643 → 9,981 actions. Agent chips now appear on days with both agents. Dead ends checked the same day: `chrome-native-host.log` (connection lifecycle only) and `coworkd.log` (VM process spawns only).

Public on GitHub since 2026-09-14: https://github.com/pseudomascot/agent-receipt (Marc created it with `gh repo create` — Claude Code's auto mode refuses to create public surfaces itself). Push after each session's commits.

User + agent identity shipped 2026-09-14. `actions.user` column added (migration in store.py; v1 fills it with the machine login, `marc`). Agent names now come from the transcript: `claude-code (claude-desktop)`, `claude-code (cli)`, `… › subagent`; entrypoint/version/sidechain kept in raw_json. Day page gets Agent and User filter chips — only rendered when there is more than one value (today: one of each, so it shows a one-line "Agent: … · User: …" instead). Summary prints Agents:/Users: lines under the same rule. RULES_VERSION bump now deletes all log rows and re-extracts (a few seconds) so identity changes reach old rows. Marc's direction: multiple agents per person and multiple people per company are the target shape; the data carries user+agent from now on so v3's multi-machine merge is trivial.

Incremental parsing + reversibility shipped 2026-09-14. Parser keeps a byte offset per transcript (`parser_state`) and only reads what was appended; a shrunken file is re-read from 0 (dedup by source_ref). `RULES_VERSION` in log_parser.py: bump it whenever classification or reversibility rules change — offsets reset and everything is re-read once; `reconcile()` (replaces prune) also re-applies rules to existing rows every refresh, deleting non-side-effects and refreshing the reversible verdict. `src/reversibility.py` assesses by rule with a stated reason (file in a git repo = yes; git commit/mkdir/pip install = yes; rm/git push/reset --hard/curl POST/gh = no; worst segment wins; unknown programs = not assessed). The reason shows under the Reversible cell on the page. 115 tests.

Page readability shipped 2026-09-14: day page groups actions by project (folder the agent worked in, from the transcript's cwd), has a type-filter bar (`?type=execute` etc.), collapses long targets behind a click-to-expand (no JavaScript — `<details>`), shows file paths relative to the project and strips the leading `cd "<dir>" &&` from commands. Day list shows top projects per day; summary has a "Projects:" line. 79 tests.

Noise reduction shipped 2026-09-14 (after v1): `src/shell_classify.py` decides whether a Bash command is read-only — conservative allowlist, quote-aware tokenizer, any redirect/heredoc/subshell/loop/unknown program counts as a side effect. Browser `computer` and `browser_batch` calls are inspected: screenshot/read-only batches are skipped; otherwise the target lists the actions that changed something (e.g. "navigate, left_click, type"). `prune()` applies current rules to rows already in the database on every refresh, so rule changes never leave stale rows. 77 tests. NOTE: after changing classification rules, restart the `.command` — the running loop has the old rules loaded and would re-insert what prune removes.

v1 steps 1-7 done; step 8 (week-long run) started 2026-09-14. Marc chose the double-click `.command` run mode. `src/run.py` starts the input monitor and statement page as child processes, opens the browser, and every 5 minutes runs parse → correlate → summary. `Agent Receipt.command` launches it; closing the Terminal window stops everything. SQLite now uses WAL + a 10s busy timeout since three processes share the file. Verified launched via Terminal: all three processes up, page serving, coverage heartbeat advancing.

Earlier: step 7 done. Step 7 (daily summary, `src/summary.py`) writes a short plain-text statement per day to `summaries/YYYY-MM-DD.txt` (git-ignored) and the same text is shown live at the top of that day's page. Deterministic, from the database only — no AI, no network. Shared read queries moved to `src/queries.py`. Run with `./.venv/bin/python src/summary.py [YYYY-MM-DD]` (default: today).

Earlier: step 6 done. Step 6 (statement page, `src/statement.py` + `src/templates/`) is a Flask app bound to 127.0.0.1:8765. `/` lists days with agent/human/unknown counts; `/day/YYYY-MM-DD` shows totals by type and attribution, the input-monitor intervals for that day (with minutes), the actions table, a separate Unknown section, and a "what this receipt covers" table on every page (Claude Code: covered; physical input: partial; Desktop chat / email / card / wallet: not covered). Verified in the browser on real data. Run with `./.venv/bin/python src/statement.py`.

Earlier: step 5 done. Step 5 (correlator, `src/correlator.py`) applies the CLAUDE.md attribution rules to every action with a 30s window; safe to re-run. Added a `coverage` table (start/end per collector, heartbeated by the input monitor every 5s) so the receipt can distinguish "no physical input" from "monitor wasn't running" — the latter is what 1,713 of 1,716 current actions say, which is the truth: the monitor has only run for ~2 minutes so far. One coverage row was backfilled by hand for that test run (10:35:32–10:37:01 on 2026-09-14).

Earlier: step 4 done. Step 4 (log parser, `src/log_parser.py`) reads every Claude Code JSONL transcript and inserts side-effect tool calls into `actions` (schema now shared in `src/store.py`; `source_ref` UNIQUE column added so re-runs never double-count). Real run on Marc's machine: 9 transcripts → 1,718 actions (838 file_write, 676 execute/Bash, ~180 browser/MCP "other", 17 post). Known judgment calls to review during the week-long run: every Bash call is logged as `execute` even if the command was read-only (ls, git status); `browser_batch` is logged as one action even when it's only screenshots. Both err toward over-reporting, which is the safer direction.

Earlier steps: steps 1-3 done. Step 2 (input monitor) verified live: 41 real events captured, thread-safety bug found and fixed. Step 3 (docs/SOURCES.md) done: Claude Code writes clean per-session JSONL transcripts to `~/.claude/projects/<project-path>/<session-uuid>.jsonl` with tool_use blocks — this is the source the log parser (step 4) will read. Claude Desktop's regular chat (non-Code tab) does NOT keep a usable local log — conversation/action data appears to sync to Anthropic's servers instead; only Electron operational logs are local. MCP tool calls appear inline in the Claude Code JSONL (no separate MCP log in use on this machine).

Permission note: the process tree for this Claude Code session runs through `/Applications/Claude.app` (not Terminal.app) — that's the app that needed Accessibility access for the input monitor, and it's what Marc granted.

## Next step
**Candidate (noted 2026-09-16, not started): Windows support.** Core already portable (SQLite, parsers, statement, alerts, connectors, pynput). Mac-only pieces to replace: `.command`/`.app` launchers (→ `Setup.bat` / PowerShell), rumps menu bar (→ pystray tray icon, or "core only" first with no tray), `osascript` notifications (→ Windows toast), Mac Calendar connector (→ "not covered"), Stop page `pgrep`/`pkill` (→ `tasklist`/`taskkill`), `open` calls, Accessibility guidance (not needed on Windows), and verify Cowork's transcript path on Windows (Claude Code and Codex use the same home-folder layout). Estimate ~2 days plus testing on a real PC. Do it when a Windows user asks.

Let it run for a week (Marc double-clicks `Agent Receipt.command` whenever he's working; closing the window stops it). Review together around 2026-09-21: read the daily summaries, look for wrong or missing lines, decide on the Bash-always-counts and browser_batch judgment calls, and whether to keep the 30s window. Then v2 planning (email / card / wallet connectors, Twilio summary).

## Blockers
None. Note for later: since Desktop-chat has no usable local log, v1's coverage is effectively "Claude Code sessions only" — the statement page should say that plainly rather than imply full coverage.

## Session log
<!-- Newest first. One entry per session: date, what got done, what's next. -->
### 2026-09-14 — Session 1
- Confirmed project name (Agent Receipt), OS (macOS). Fixed LICENSE placeholder. Git repo initialized.
- Built and verified all of v1 in one session: input monitor (step 2), log sources documented (step 3), log parser (step 4), correlator + coverage tracking (step 5), statement page (step 6), daily summary (step 7), and the `.command` runner for the week-long trial (step 8 started). 23 tests.
- Marc's early concern — not wanting input logged while no agent is running — shaped two things: code was written and tested before anything ran live, and the monitor only runs while the `.command` window is open.
- Real-world checks: 41 events in the first live monitor test (found and fixed a thread-safety bug); ~1,700 actions parsed from 9 transcripts; the correlator correctly flagged 3 actions where Marc was typing within 30s of an agent action; today's summary says 69 of 72 actions happened while the monitor was off — which is true, and the point.
- Findings worth remembering: Accessibility permission belongs to the *launching app* (Claude.app for this session, Terminal for the `.command`); Claude Desktop chat has no usable local log, so v1 covers Claude Code only.
- Next: run for a week, review ~2026-09-21.
